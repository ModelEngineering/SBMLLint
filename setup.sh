# For SbStar. These installs are
# required to run the demo
# Note that you must have conda (or miniconda) installed to 
# run this script.
conda install -c sys-bio tellurium
conda install -c SBMLTeam python-libsbml
